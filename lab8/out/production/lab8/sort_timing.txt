Q: Is one sorting algorithm always faster than another?
A: One sorting algorithm is always faster than another. I haven't seen an instance where an algorithm was the same as another. However, one sorting algorithm isn't necessarily faster consistently throughout the graph.

Q: Above we said that BubbleSort, WipingBubbleSort, and InsertionSort each had the same Theta(N^2) asymptotic time complexity. How can you explain the differences in the plots for these three algorithms?
A: These algorithms both were exponential functions and on the graph one can see that these 3 algorithms were getting exponentially larger. However, BubbleSort's slope was noticeably steeper compared to WipingBubbleSort and InsertionSort. As well as for the WipingBubbleSort slope being noticeably larger than the InsertionSort line.

Q: What information can we gain from empirical analysis of algorithms which might not be as noticeable in asymptotical bounds?
A: We can see that even though the above algorithms had the same asymptotical bound, that in the plots, their lines were very different. BubbleSort was immensely slower than the other two and so we are able to see in practice rather than in theory how fast these algorithms actually work.

Q: For any given sorting algorithm, does increasing the array size always mean the sorting takes longer?
A: The sorting does take longer when you increase the array size.

Q: How does changing nrepeats change the plot?
A: By changing the nrepeats arguments, you essentially change the readability of the plots. As nrepeats decreases, it is unclear as to which algorithm is faster.

Q: Is your plot the exact same as your partner's plot, even with the same values of ntrials, by, and nrepeats?
A: I did not have a partner but by retesting, if the numbers were in the higher range then it was similar but the graphs were never exact.

Q: Optional: Look at the source code for BubbleSorter to WipingBubbleSorter. After looking at the plots, can you intuitively explain why WipingBubbleSorter is usually 2x as fast as BubbleSorter? (Hint: Consider the immobility of some elements when the swapping passes are single directional (i.e. only going forward), and how this "Wiping" strategy helps deal with that issue.) Can you come up with an example that shows the difference in runtime?
A: